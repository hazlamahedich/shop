[pytest]
# Pytest configuration for e-commerce provider tests
# These tests run WITHOUT database dependencies
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
